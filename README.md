
  # Customer Signal

  This is a code bundle for Customer Signal. The original project is available at https://www.figma.com/design/1LcXUasPD4LBUSZ7m8d9gB/Customer-Signal.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  